# PDCA Cycle: sanmei-cli implementation

- Status: in_progress
- Created: 2026-03-02T12:00:00+09:00
- Updated: 2026-03-02T12:00:00+09:00

## Dependency Analysis

```
Wave 1: Task 1 (scaffold) — foundation, must be first
Wave 2: Task 2 + Task 3 (parallel) — independent files
Wave 3: Task 4 + Task 5 (parallel) — independent files
Wave 4: Task 6 → Task 7 → Task 8 → Task 9 (sequential) — all modify main.py/text.py
Wave 5: Task 10 (quality gate) — depends on all
```

---

## Task: 1. Package Scaffold & Workspace Setup
- ID: task-1
- Status: plan
- Dependencies: none
- Iteration: 1

### Plan
- DoD:
  - `apps/sanmei-cli/pyproject.toml` exists with correct config
  - Source package dirs created (`__init__.py` files)
  - Root `pyproject.toml` includes `apps/*` in workspace
  - `Justfile` updated with sanmei-cli targets
  - `uv sync` succeeds
- Steps:
  1. Create `apps/sanmei-cli/pyproject.toml`
  2. Create empty source files (`__init__.py`, `main.py` placeholder)
  3. Update root `pyproject.toml` workspace members
  4. Update `Justfile` with sanmei-cli targets
  5. Run `uv sync` to verify
- Test Strategy: `uv sync` success = sufficient for scaffold

---

## Task: 2. Test Fixtures & Gender Type
- ID: task-2
- Status: plan
- Dependencies: task-1
- Iteration: 1

### Plan
- DoD:
  - `conftest.py` provides meishiki, taiun_chart, nenun_list, isouhou_result fixtures
  - `GenderType` handles all aliases (男/male/m, 女/female/f) case-insensitively
  - All `test_gender_type.py` tests pass
- Steps:
  1. Create `tests/conftest.py` with fixtures
  2. Write `tests/test_gender_type.py` (TDD: tests first)
  3. Run tests → expect FAIL
  4. Create `src/sanmei_cli/types.py` with GenderType
  5. Run tests → expect PASS
- Test Strategy: Parametrized tests for all gender aliases + error case

---

## Task: 3. Main CLI Group
- ID: task-3
- Status: plan
- Dependencies: task-1
- Iteration: 1

### Plan
- DoD:
  - `cli` group with `--json` and `--school` options
  - `build_datetime` helper function
  - `--help` shows 算命学CLI
  - No-args shows help
  - All `test_main.py` tests pass
- Steps:
  1. Write `tests/test_main.py` (TDD: tests first)
  2. Run tests → expect FAIL
  3. Implement `main.py`
  4. Run tests → expect PASS
- Test Strategy: CliRunner integration tests

---

## Task: 4. Meishiki Text Formatter
- ID: task-4
- Status: plan
- Dependencies: task-2
- Iteration: 1

### Plan
- DoD:
  - `format_meishiki` returns complete text with all sections
  - All `test_text_formatter.py::TestFormatMeishiki` tests pass
- Steps:
  1. Write `tests/test_text_formatter.py` (TDD)
  2. Run tests → expect FAIL
  3. Implement `formatters/text.py`
  4. Run tests → expect PASS
- Test Strategy: Check presence of each section header and key fields

---

## Task: 5. JSON Formatter
- ID: task-5
- Status: plan
- Dependencies: task-2
- Iteration: 1

### Plan
- DoD:
  - `to_json` handles Pydantic models and lists
  - JSON output is valid and contains expected keys
  - Japanese text preserved (ensure_ascii=False)
  - All `test_json_formatter.py` tests pass
- Steps:
  1. Write `tests/test_json_formatter.py` (TDD)
  2. Run tests → expect FAIL
  3. Implement `formatters/json_fmt.py`
  4. Run tests → expect PASS
- Test Strategy: Parse JSON output and verify structure

---

## Task: 6. Meishiki Command
- ID: task-6
- Status: plan
- Dependencies: task-3, task-4, task-5
- Iteration: 1

### Plan
- DoD:
  - `sanmei meishiki 2000-01-15` produces text output
  - `--time` option works
  - `--json` flag produces valid JSON
  - Invalid/out-of-range dates error properly
  - All `test_meishiki_cmd.py` tests pass
- Steps:
  1. Write `tests/test_meishiki_cmd.py` (TDD)
  2. Run tests → expect FAIL
  3. Implement `commands/meishiki.py`
  4. Register command in `main.py`
  5. Run tests → expect PASS
- Test Strategy: CliRunner integration tests (normal + error cases)

---

## Task: 7. Taiun Formatter & Command
- ID: task-7
- Status: plan
- Dependencies: task-3, task-4, task-5
- Iteration: 1

### Plan
- DoD:
  - `format_taiun` in text.py produces formatted output
  - `sanmei taiun` command works with gender option
  - JSON output works
  - All taiun tests pass
- Steps:
  1. Add taiun formatter tests to `test_text_formatter.py`
  2. Write `tests/test_taiun_cmd.py` (TDD)
  3. Run tests → expect FAIL
  4. Implement `format_taiun` in `formatters/text.py`
  5. Implement `commands/taiun.py`
  6. Register command in `main.py`
  7. Run tests → expect PASS
- Test Strategy: Formatter unit tests + CliRunner integration tests

---

## Task: 8. Nenun Formatter & Command
- ID: task-8
- Status: plan
- Dependencies: task-3, task-4, task-5
- Iteration: 1

### Plan
- DoD:
  - `format_nenun` in text.py produces formatted output
  - `sanmei nenun` command works with --from/--to options
  - JSON output works
  - All nenun tests pass
- Steps:
  1. Add nenun formatter tests to `test_text_formatter.py`
  2. Write `tests/test_nenun_cmd.py` (TDD)
  3. Run tests → expect FAIL
  4. Implement `format_nenun` in `formatters/text.py`
  5. Implement `commands/nenun.py`
  6. Register command in `main.py`
  7. Run tests → expect PASS
- Test Strategy: Formatter unit tests + CliRunner integration tests

---

## Task: 9. Isouhou Formatter & Command
- ID: task-9
- Status: plan
- Dependencies: task-3, task-4, task-5
- Iteration: 1

### Plan
- DoD:
  - `format_isouhou` in text.py produces formatted output
  - `sanmei isouhou` command works
  - JSON output works
  - Empty interactions handled ("相互作用なし")
  - All isouhou tests pass
- Steps:
  1. Add isouhou formatter tests to `test_text_formatter.py`
  2. Write `tests/test_isouhou_cmd.py` (TDD)
  3. Run tests → expect FAIL
  4. Implement `format_isouhou` in `formatters/text.py`
  5. Implement `commands/isouhou.py`
  6. Register command in `main.py`
  7. Run tests → expect PASS
- Test Strategy: Formatter unit tests + CliRunner integration tests

---

## Task: 10. Quality Gate
- ID: task-10
- Status: plan
- Dependencies: task-6, task-7, task-8, task-9
- Iteration: 1

### Plan
- DoD:
  - All tests pass with coverage >= 80%
  - Ruff lint clean
  - Mypy clean
  - `just check` passes (full project)
  - Smoke test all 4 commands
- Steps:
  1. Run all tests with coverage
  2. Run ruff lint/format check
  3. Run mypy
  4. Run `just check`
  5. Manual smoke test all commands
  6. Fix any issues found
- Test Strategy: Full quality gate validation
