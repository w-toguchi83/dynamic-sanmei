# PDCA Cycle: 使命星（しめいせい）算出ロジック追加

- Status: completed
- Created: 2026-03-02T20:00:00+09:00
- Updated: 2026-03-02T20:30:00+09:00

## Task: sanmei-core に使命星算出ロジックを追加
- ID: task-1
- Status: completed
- Dependencies: none
- Iteration: 1

### Plan

**概要:** 使命星は「年干の干合相手」と「日干」から十大主星を引く計算。

**算出ロジック:**
1. 年干の干合相手を取得（干合表: 甲↔己, 乙↔庚, 丙↔辛, 丁↔壬, 戊↔癸）
2. `school.determine_major_star(day_stem, kangou_partner)` で十大主星を取得
3. 結果は MajorStar 型の単一値

**DoD:**
- [x] `calculators/shimeisei.py` に `calculate_shimeisei()` 関数を作成
- [x] `tables/isouhou.py` に `get_kangou_partner()` ヘルパー関数を追加
- [x] `domain/meishiki.py` の `Meishiki` に `shimeisei: MajorStar` フィールドを追加
- [x] `calculators/meishiki_calculator.py` から `calculate_shimeisei()` を呼び出し
- [x] `__init__.py` に `calculate_shimeisei` をエクスポート
- [x] テスト: 太郎さんの例（庚×癸=調舒星）を含む検証
- [x] テスト: 全10干の干合相手が正しいことを検証
- [x] `just test-pkg sanmei-core` 全テスト合格
- [x] `just check` （lint + typecheck + test）合格

### Do Log
#### Iteration 1
1. `get_kangou_partner()` を TenStem((stem.value + 5) % 10) で実装（数学的に干合は+5の関係）
2. テスト12件: 全10干の干合相手 + 全干カバレッジ + 二重干合=自分自身
3. `calculate_shimeisei()` を実装: kangou_partner を取得して determine_major_star に委譲
4. テスト22件: 太郎例 + 20パターン（日干=甲/庚 × 年干=全10干）+ 型チェック
5. Meishiki に shimeisei フィールド追加、MeishikiCalculator から呼び出し
6. __init__.py にエクスポート追加
7. 既存テスト11件が shimeisei フィールド欠落で失敗 → ダミー値追加で修正

### Check Log
#### Iteration 1
- Tests: PASS (605 passed, 0 failed)
- Lint: FAIL → import 順序エラー（ruff I001）→ 修正後 PASS
- TypeCheck: PASS (全3パッケージ)
- DoD: 全項目 PASS
- Result: PASS（Adjust 1回: import 順序修正）

### Adjust Log
#### Iteration 1
- Failure: ruff I001 import sort error in __init__.py
- Root Cause: shimeisei import を pillar_calculator の前に挿入してしまった
- Plan Changes: なし（単純な挿入位置ミス）

### Retrospective
#### 成功
- 干合の数学的性質（+5 mod 10）を活用し、テーブルなしで実装できた
- 既存の determine_major_star() を再利用し、新テーブル不要
- 書籍テーブルの十大主星表が既存コードと異なる索引方式（行列転置的な関係）であることを事前に分析し、テスト期待値の誤りを防いだ

#### 失敗と再発防止
| 失敗 | 根本原因 | 再発防止策 |
|------|----------|------------|
| テスト期待値を最初に誤算出 | 書籍テーブルの索引方式と determine_major_star の入力順序の違いを見落とし | 五行関係を手計算で確認してからテスト値を決定する |
| import 順序エラー | アルファベット順を確認せずに挿入 | 新 import は ruff check で事前確認 |
| 既存テスト11件失敗 | Meishiki にフィールド追加時の波及を忘れた | MEMORY の「フィールド追加は全テストに波及」を再確認する |

---

## Task: sanmei-cli に使命星の出力を追加
- ID: task-2
- Status: completed
- Dependencies: task-1
- Iteration: 1

### Plan

**概要:** `format_meishiki()` の蔵干セクション後に使命星を表示。

**表示形式:**
```
【使命星】 調舒星
```

**DoD:**
- [x] `formatters/text.py` の `format_meishiki()` に使命星セクションを追加（蔵干の後）
- [x] `formatters/json_fmt.py` にも shimeisei が含まれることを確認（Pydantic model_dump で自動）
- [x] テスト: `test_text_formatter.py` に使命星セクションのテスト追加
- [x] `just test-pkg sanmei-cli` 全テスト合格
- [x] `just check` 合格

### Do Log
#### Iteration 1
1. `format_meishiki()` に `【使命星】 {meishiki.shimeisei.value}` を蔵干セクション後に追加
2. テスト2件: セクション存在確認 + 蔵干と十大主星の間に配置されていることの確認
3. 全67テスト合格

### Check Log
#### Iteration 1
- Tests: PASS (67 passed, 0 failed)
- Lint: PASS
- TypeCheck: PASS
- DoD: 全項目 PASS
- Result: PASS

### Retrospective
#### 成功
- Task 1 の成果をそのまま活用し、最小限の変更で CLI 出力を追加できた
- Pydantic model_dump で JSON 出力は自動対応
