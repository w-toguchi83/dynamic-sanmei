Base directory for this skill: /Users/toguchi/.claude/skills/task-pdca

# Task PDCA

PDCAサイクルでタスクを実装・完成させるプロセススキル。

**コア原則:** Plan → Do → Check → Adjust のループで品質を担保し、振り返りで再発防止、Feedback でナレッジを永続化する。

## サブコマンド

| コマンド | 説明 |
|----------|------|
| `/task-pdca` | 新規PDCAサイクルを開始（タスク情報を入力） |
| `/task-pdca continue` | 中断したサイクルを再開 |
| `/task-pdca clean` | 完了済みサイクルの状態ファイルを削除 |

## いつ使うか

```dot
digraph when_to_use {
    "タスク実装の依頼?" [shape=diamond];
    "中断からの再開?" [shape=diamond];
    "完了済みの清掃?" [shape=diamond];
    "/task-pdca で新規開始" [shape=box];
    "/task-pdca continue" [shape=box];
    "/task-pdca clean" [shape=box];
    "別のスキルを検討" [shape=box];

    "タスク実装の依頼?" -> "中断からの再開?" [label="no"];
    "タスク実装の依頼?" -> "/task-pdca で新規開始" [label="yes"];
    "中断からの再開?" -> "完了済みの清掃?" [label="no"];
    "中断からの再開?" -> "/task-pdca continue" [label="yes"];
    "完了済みの清掃?" -> "/task-pdca clean" [label="yes"];
    "完了済みの清掃?" -> "別のスキルを検討" [label="no"];
}
```

## 全体フロー

```dot
digraph pdca_flow {
    rankdir=TB;

    "タスク入力を受け取る" [shape=doublecircle];
    "依存関係を分析" [shape=box];
    "独立タスクあり?" [shape=diamond];
    "並列でサブエージェントに委任" [shape=box];
    "逐次実行" [shape=box];

    subgraph cluster_pdca {
        label="PDCAサイクル（タスクごと）";

        "Plan: 計画作成" [shape=box];
        "Do: 実装" [shape=box];
        "Check: 検証" [shape=box];
        "Check 合格?" [shape=diamond];
        "Adjust: 計画修正" [shape=box];
        "コミット" [shape=box];
        "振り返り: 再発防止策" [shape=box];
    }

    "Feedback: ナレッジ保存 + 最終報告" [shape=box];
    "完了" [shape=doublecircle];

    "タスク入力を受け取る" -> "依存関係を分析";
    "依存関係を分析" -> "独立タスクあり?";
    "独立タスクあり?" -> "並列でサブエージェントに委任" [label="yes"];
    "独立タスクあり?" -> "逐次実行" [label="no"];
    "並列でサブエージェントに委任" -> "Plan: 計画作成";
    "逐次実行" -> "Plan: 計画作成";

    "Plan: 計画作成" -> "Do: 実装";
    "Do: 実装" -> "Check: 検証";
    "Check: 検証" -> "Check 合格?" ;
    "Check 合格?" -> "Adjust: Plan修正 + 原因分析" [label="no"];
    "Adjust: Plan修正 + 原因分析" -> "Do: 実装" [label="修正箇所を再実行"];
    "Check 合格?" -> "コミット" [label="yes"];
    "コミット" -> "振り返り: 再発防止策";
    "振り返り: 再発防止策" -> "Feedback: ナレッジ保存 + 最終報告";
    "Feedback: ナレッジ保存 + 最終報告" -> "完了";
}
```

## 各フェーズの詳細

### 1. Plan（計画）

**目的:** 何をどう実装するか、完了条件（DoD）を明確にする。

**手順:**
1. タスクの要件を分析（不明点があれば人間に質問）
2. 完了条件（Definition of Done）を定義
3. 実装ステップを分解（粒度: 各ステップ15分以内）
4. テスト方針を決定
5. 状態ファイルに Plan を記録

**Plan の記録形式（状態ファイル内）:**
```
## Plan
- DoD: [完了条件のリスト]
- Steps:
  1. [ステップ1]
  2. [ステップ2]
  ...
- Test Strategy: [テスト方針]
```

**スキル連携:** 要件が複雑な場合は `superpowers:brainstorming` で設計を深掘りし、`superpowers:writing-plans` で詳細計画を作成。

### 2. Do（実行）

**目的:** Plan に沿って実装する。

**手順:**
1. 状態ファイルの phase を `do` に更新
2. Plan のステップを順に実行
3. テストを書き、実装する（**REQUIRED:** `superpowers:test-driven-development` に従う）
4. 各ステップの完了時に状態ファイルを更新

**サブエージェント委任時のプロンプト構造:**
```
タスク: [タスク名]

## タスク説明
[タスクの全文 - ファイルを読ませず直接渡す]

## Plan
[Plan フェーズで作成した計画]

## 完了条件（DoD）
[チェックリスト形式]

## コンテキスト
[アーキテクチャ、依存関係、既存パターン]

## 制約
- Plan のステップに従うこと
- テストファースト（TDD）で実装
- 完了条件をすべて満たすこと
- 不明点があれば質問すること
```

### 3. Check（検証）

**目的:** Do の成果が Plan の完了条件を満たすか厳格に検証する。

**鉄則: 検証なしに「完了」と言わない。**

**手順:**
1. テスト実行 → 全テスト合格を確認（出力を確認）
2. リント/型チェック → エラー0を確認（出力を確認）
3. DoD チェックリスト → 各項目を 1 つずつ検証
4. 検証結果を状態ファイルに記録

**Check の記録形式:**
```
## Check (Iteration N)
- Tests: PASS/FAIL [詳細]
- Lint: PASS/FAIL [詳細]
- DoD:
  - [x] 条件1: PASS
  - [ ] 条件2: FAIL - [理由]
- Result: PASS/FAIL
```

**スキル連携:** `superpowers:verification-before-completion` の鉄則に従う。証拠なしに成功を主張しない。

### 4. Adjust（修正）

**目的:** Check で不合格だった場合、Plan 自体を見直す。

**重要: コードだけ直すのではなく、Plan を修正してから Do に戻る。**

```dot
digraph adjust {
    "Check 失敗" [shape=ellipse];
    "失敗原因を分析" [shape=box];
    "Plan の問題?" [shape=diamond];
    "Plan のステップ/DoD を修正" [shape=box];
    "実装のバグ?" [shape=diamond];
    "バグ修正ステップを Plan に追加" [shape=box];
    "修正した Plan の Do ステップを実行" [shape=box];

    "Check 失敗" -> "失敗原因を分析";
    "失敗原因を分析" -> "Plan の問題?";
    "Plan の問題?" -> "Plan のステップ/DoD を修正" [label="yes"];
    "Plan の問題?" -> "実装のバグ?" [label="no"];
    "実装のバグ?" -> "バグ修正ステップを Plan に追加" [label="yes"];
    "実装のバグ?" -> "環境/テスト問題 → 人間に相談" [label="no"];
    "Plan のステップ/DoD を修正" -> "修正した Plan の Do ステップを実行";
    "バグ修正ステップを Plan に追加" -> "修正した Plan の Do ステップを実行";
}
```

**Adjust のフロー:** Adjust は常に Plan を修正した上で、修正部分の Do → Check を再実行する。コードだけ直して Check をスキップするのは禁止。

**Adjust の記録形式:**
```
## Adjust (Iteration N)
- Failure: [Check で失敗した内容]
- Root Cause: [根本原因]
- Plan Changes: [Plan への修正内容]
```

**安全弁:** 3回以上 Adjust ループした場合、人間に相談する。無限ループを避ける。

### 5. 振り返り（Retrospective）

**目的:** 成功・失敗を分析し、再発防止策を立てる。

**Check 合格後、コミット後に必ず実行。**

**スコープ:**
- **タスクごとの振り返り:** 各タスクの PDCA 完了後に実施。状態ファイルの各タスクセクションに記録。
- **サイクル全体の振り返り:** 全タスク完了後、Feedback フェーズの前に実施。タスク横断の学びを統合。

**手順:**
1. **何がうまくいったか** — 成功パターンを特定
2. **何が失敗したか** — 各 Adjust イテレーションの原因を分析
3. **再発防止策** — 同じ失敗を繰り返さないための具体的アクション
4. 振り返り結果を状態ファイルに記録

**振り返りの記録形式:**
```
## Retrospective
### 成功
- [成功パターン]

### 失敗と再発防止
| 失敗 | 根本原因 | 再発防止策 |
|------|----------|------------|
| [何が起きた] | [なぜ] | [今後どうする] |

### 学び
- [セッションをまたいで有用な知見]
```

**これを省略しない。** 振り返りのない PDCA は単なる作業ループ。

### 6. Feedback（報告・ナレッジ保存）

**目的:** ナレッジを永続化し、人間に最終報告する。

**手順:**
1. **ナレッジ保存:** 振り返りの「学び」と「再発防止策」をメモリファイルに保存
   - 保存先: 自動メモリディレクトリ内の `pdca-learnings.md`（Claude Code が使用するプロジェクトメモリディレクトリ。通常 `~/.claude/projects/` 配下にプロジェクトパスから生成される）
   - 既存の学びがあれば追記（重複しない）
   - 日付とサイクル名をヘッダーとして記録
2. **状態ファイルを `Status: completed` に更新**
3. **PDCA 状態ファイルをコミット:** `.claude/pdca/` 配下の状態ファイルを stage してコミットする
   - コミットメッセージ: `chore: add PDCA cycle record for [サイクル説明]`
   - 実装コードのコミットとは別にする（PDCA 記録は作業メタデータ）
   - **これを省略しない。** 状態ファイルはチームのナレッジ資産であり、コミット漏れは学びの消失を意味する
4. **最終報告:** 人間に以下を簡潔に表示

**最終報告フォーマット:**
```
## PDCA 完了報告

### 実装したタスク
- [タスク1]: [概要] ✅
- [タスク2]: [概要] ✅

### PDCA サイクル統計
- イテレーション回数: N
- Adjust 回数: M

### 主な学び
- [学び1]
- [学び2]

### 再発防止策
- [対策1]
- [対策2]
```

## 状態管理

### 状態ファイルの場所

```
.claude/pdca/
  cycle-{YYYYMMDD-HHMMSS}.md    # サイクルごとの状態ファイル
```

プロジェクトルートの `.claude/pdca/` ディレクトリに保存。

### 状態ファイルのフォーマット

```markdown
# PDCA Cycle: [説明]

- Status: in_progress | completed
- Created: [ISO日時]
- Updated: [ISO日時]

## Task: [タスク名]
- ID: task-1
- Status: plan | do | check | adjust | retrospective | completed
- Dependencies: [none | task-2, task-3]
- Iteration: 1

### Plan
[Plan 内容]

### Do Log
#### Iteration 1
[Do の記録]

### Check Log
#### Iteration 1
[Check の記録]

### Adjust Log
#### Iteration 1
[Adjust の記録（該当する場合）]

### Retrospective
[振り返り内容]

---

## Task: [タスク2名]
...
```

### フェーズ遷移ルール

状態ファイルのタスク Status は以下のタイミングで更新する:

| 遷移 | タイミング |
|------|-----------|
| `plan` → `do` | Plan を状態ファイルに記録した直後 |
| `do` → `check` | Do の実装が完了した直後 |
| `check` → `adjust` | Check が FAIL だった直後 |
| `adjust` → `do` | Adjust で Plan を修正した直後（再実行前） |
| `check` → `retrospective` | Check が PASS でコミット完了後 |
| `retrospective` → `completed` | 振り返りを記録した後 |

**遷移のたびに `Updated` タイムスタンプも更新する。**

### continue（再開）

`/task-pdca continue` 実行時:
1. `.claude/pdca/` から `Status: in_progress` のサイクルファイルを探す
2. 各タスクの現在フェーズを読み取る
3. 中断地点から再開（例: do フェーズの途中なら do から）
4. 複数のサイクルがある場合、最新のものを選択（または人間に確認）

### clean（清掃）

`/task-pdca clean` 実行時:
1. `.claude/pdca/` から `Status: completed` のサイクルファイルを検索
2. 一覧を表示し、人間に確認を取る
3. 確認後、該当ファイルを削除

## 並列実行

```dot
digraph parallel {
    "タスク一覧" [shape=box];
    "依存関係グラフを構築" [shape=box];
    "独立タスクを特定" [shape=box];
    "2つ以上の独立タスク?" [shape=diamond];
    "Agent ツールで並列ディスパッチ" [shape=box];
    "逐次実行" [shape=box];
    "全サブエージェント完了待ち" [shape=box];
    "結果を統合・Check" [shape=box];

    "タスク一覧" -> "依存関係グラフを構築";
    "依存関係グラフを構築" -> "独立タスクを特定";
    "独立タスクを特定" -> "2つ以上の独立タスク?";
    "2つ以上の独立タスク?" -> "Agent ツールで並列ディスパッチ" [label="yes"];
    "2つ以上の独立タスク?" -> "逐次実行" [label="no"];
    "Agent ツールで並列ディスパッチ" -> "全サブエージェント完了待ち";
    "全サブエージェント完了待ち" -> "結果を統合・Check";
}
```

**並列実行時のルール:**
- 各サブエージェントは独立した worktree で作業（`isolation: "worktree"` を使用）
- サブエージェントには Plan 全文 + DoD + コンテキストを渡す（ファイルを読ませない）
- 同じファイルを編集する可能性がある場合は並列にしない
- 各サブエージェントが独自に Do → Check → Adjust ループを実行し、合格したらコミット
- **状態ファイルはメインエージェントのみが更新する。** サブエージェントは結果を報告し、メインが状態ファイルに反映する
- メインエージェントは全サブエージェント完了後に以下を実施:
  1. 各 worktree の変更をメインブランチにマージ
  2. マージ後の統合テスト実行（`just check` 等）
  3. 統合テスト合格を確認して状態ファイルを更新

**サブエージェントへのプロンプト:**
```
あなたは PDCA サイクルの Do フェーズを実行するサブエージェントです。

## タスク
[タスク全文]

## Plan
[計画全文]

## 完了条件（DoD）
[チェックリスト]

## 実行手順
1. Plan のステップに従って実装（TDD で）
2. 全テスト合格を確認
3. リント/型チェック合格を確認
4. DoD の各項目を検証
5. 問題があれば Plan を修正して再実行（最大3回）
6. コミットする
7. 結果を報告:
   - 実装内容
   - テスト結果（出力を含む）
   - DoD チェックリスト結果
   - 問題があった場合の Adjust 記録
```

## スキル連携

| フェーズ | 連携スキル | 用途 |
|----------|------------|------|
| Plan | `superpowers:brainstorming` | 要件が複雑な場合の設計深掘り |
| Plan | `superpowers:writing-plans` | 詳細実装計画の作成 |
| Do | `superpowers:test-driven-development` | TDD で実装 |
| Do | `superpowers:dispatching-parallel-agents` | 独立タスクの並列実行 |
| Check | `superpowers:verification-before-completion` | 厳格な検証 |
| Adjust | `superpowers:systematic-debugging` | 失敗原因の調査 |
| コミット | `superpowers:finishing-a-development-branch` | ブランチ完了時 |

## レッドフラグ — 止まれ

- Check せずに「完了」と言おうとしている
- テスト出力を確認せずに「テスト合格」と主張
- Adjust で Plan を見直さずコードだけ修正
- 振り返りを「特になし」で済ませようとしている
- 3回以上 Adjust ループしているのに人間に相談していない
- 独立タスクを逐次処理している（並列可能なのに）
- 状態ファイルを更新していない
- ナレッジ保存をスキップしようとしている
- PDCA 状態ファイルをコミットせずにセッションを終了しようとしている

## よくある間違い

| 間違い | 正しい対応 |
|--------|------------|
| Check なしで完了宣言 | 必ずテスト・リント・DoD を実行して確認 |
| Adjust でコードだけ修正 | Plan 自体を見直す。なぜ計画が不十分だったか考える |
| 振り返りを省略 | 必須。失敗がなくても「何がうまくいったか」を記録 |
| ナレッジ保存忘れ | Feedback フェーズで必ずメモリファイルに保存 |
| PDCA 状態ファイルのコミット漏れ | Feedback フェーズで `.claude/pdca/` を必ず stage & commit する |
| 状態ファイル未更新 | 各フェーズ遷移時に必ず更新。continue の命綱 |
| 並列可能なのに逐次処理 | 依存関係がなければ Agent ツールで並列実行 |


ARGUMENTS: 十大主星,十二大従星の算出についても算命学のロジックではない可能性が高いです（または算出ロジックのバグ）。見直して、修正してください。
以下の参考は書籍の記述です。
```
# 2　宿命星の求め方（p.52）

## 性格と運勢を表わす
宿命星は、性格を表わす「十大主星」と運勢を表わす「十二大従星」の2種類あります。  
生年月日から出した命式の「日干」を軸にして、

（※読み方）  
んくらせい、天胡星（てんこせい）という読み方になっています。

宿命図が持っている星によって、自分の内面をより具体的に知ることができます。自分が潜在的に持っている才能や性格、本能や価値観、好きな人・嫌いな人、好きな食べ物・嫌いな食べ物までわかります。もちろん生き方までも知ることができます。今までなんとなく手探りで「これでよいのだろうか」と自問自答しながら生きてきた人は、自分を知ることで自信が持てるはずです。

ほかの五つの「十干」から生まれた星が「十大主星」です。同じように、ほかの三つの十二支から生まれた星が「十二大従星」になります。

一つの宿命図に、十大主星が五つと十二大従星が三つ、計八つの星が出てきます。

## 「十大主星」と「十二大従星」の出し方
左ページ・一番上の表で、①〜⑤は「十大主星」の場所、A〜Cは「十二大従星」の場所を示しています。

①は「日干」と「年干」が交わる場所の星  
②は「日干」と「月干」が交わる場所の星  
③は「日干」と「年支の蔵干」が交わる場所の星  
④は「日干」と「月支の蔵干」が交わる場所の星  
⑤は「日干」と「日支の蔵干」が交わる場所の星  

Aは「日干」と「年支」が交わる場所の星  
Bは「日干」と「月支」が交わる場所の星  
Cは「日干」と「日支」が交わる場所の星  


---

# 第二章　宿命図からわかる性格（p.53）

## 【「十大主星」と「十二大従星」の出し方】（配置図）
┌─────┬─────┬─────┐
│       │  ①   │   A  │
├─────┼─────┼─────┤
│  ⑤   │  ④   │  ③   │
├─────┼─────┼─────┤
│   C  │  ②   │   B  │
└─────┴─────┴─────┘
---

## 【十大主星表】

| 他干＼日干 | 甲 | 乙 | 丙 | 丁 | 戊 | 己 | 庚 | 辛 | 壬 | 癸 |
|---|---|---|---|---|---|---|---|---|---|---|
| 甲 | 貫索星 | 石門星 | 龍高星 | 玉堂星 | 車騎星 | 牽牛星 | 禄存星 | 司禄星 | 鳳閣星 | 調舒星 |
| 乙 | 石門星 | 貫索星 | 玉堂星 | 龍高星 | 牽牛星 | 車騎星 | 司禄星 | 禄存星 | 調舒星 | 鳳閣星 |
| 丙 | 鳳閣星 | 調舒星 | 貫索星 | 石門星 | 龍高星 | 玉堂星 | 車騎星 | 牽牛星 | 禄存星 | 司禄星 |
| 丁 | 調舒星 | 鳳閣星 | 石門星 | 貫索星 | 玉堂星 | 龍高星 | 牽牛星 | 車騎星 | 司禄星 | 禄存星 |
| 戊 | 禄存星 | 司禄星 | 鳳閣星 | 調舒星 | 貫索星 | 石門星 | 龍高星 | 玉堂星 | 車騎星 | 牽牛星 |
| 己 | 司禄星 | 禄存星 | 調舒星 | 鳳閣星 | 石門星 | 貫索星 | 玉堂星 | 龍高星 | 牽牛星 | 車騎星 |
| 庚 | 車騎星 | 牽牛星 | 禄存星 | 司禄星 | 鳳閣星 | 調舒星 | 貫索星 | 石門星 | 龍高星 | 玉堂星 |
| 辛 | 牽牛星 | 車騎星 | 司禄星 | 禄存星 | 調舒星 | 鳳閣星 | 石門星 | 貫索星 | 玉堂星 | 龍高星 |
| 壬 | 龍高星 | 玉堂星 | 車騎星 | 牽牛星 | 禄存星 | 司禄星 | 鳳閣星 | 調舒星 | 貫索星 | 石門星 |
| 癸 | 玉堂星 | 龍高星 | 牽牛星 | 車騎星 | 司禄星 | 禄存星 | 調舒星 | 鳳閣星 | 石門星 | 貫索星 |

---

## 【十二大従星表】

| 支＼日干 | 甲 | 乙 | 丙 | 丁 | 戊 | 己 | 庚 | 辛 | 壬 | 癸 |
|---|---|---|---|---|---|---|---|---|---|---|
| 子 | 天恍星 | 天胡星 | 天報星 | 天馳星 | 天報星 | 天馳星 | 天極星 | 天貴星 | 天将星 | 天禄星 |
| 丑 | 天南星 | 天堂星 | 天印星 | 天庫星 | 天印星 | 天庫星 | 天庫星 | 天印星 | 天堂星 | 天南星 |
| 寅 | 天禄星 | 天将星 | 天貴星 | 天極星 | 天貴星 | 天極星 | 天馳星 | 天報星 | 天胡星 | 天恍星 |
| 卯 | 天将星 | 天禄星 | 天恍星 | 天胡星 | 天恍星 | 天胡星 | 天馳星 | 天報星 | 天胡星 | 天恍星 |
| 辰 | 天堂星 | 天南星 | 天南星 | 天堂星 | 天南星 | 天堂星 | 天印星 | 天庫星 | 天庫星 | 天印星 |
| 巳 | 天胡星 | 天恍星 | 天禄星 | 天将星 | 天禄星 | 天将星 | 天貴星 | 天極星 | 天馳星 | 天報星 |
| 午 | 天極星 | 天貴星 | 天将星 | 天禄星 | 天将星 | 天禄星 | 天恍星 | 天胡星 | 天報星 | 天馳星 |
| 未 | 天庫星 | 天印星 | 天堂星 | 天南星 | 天堂星 | 天南星 | 天南星 | 天堂星 | 天印星 | 天庫星 |
| 申 | 天馳星 | 天報星 | 天胡星 | 天恍星 | 天胡星 | 天恍星 | 天禄星 | 天将星 | 天貴星 | 天極星 |
| 酉 | 天報星 | 天馳星 | 天極星 | 天貴星 | 天極星 | 天貴星 | 天将星 | 天禄星 | 天恍星 | 天胡星 |
| 戌 | 天印星 | 天庫星 | 天庫星 | 天印星 | 天庫星 | 天印星 | 天堂星 | 天南星 | 天南星 | 天堂星 |
| 亥 | 天貴星 | 天極星 | 天馳星 | 天報星 | 天馳星 | 天報星 | 天胡星 | 天恍星 | 天禄星 | 天将星 |
```

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me chronologically analyze the conversation:

1. The user invoked `/task-pdca` with a detailed request to review and fix the 十大主星 (Ten Major Stars) and 十二大従星 (Twelve Subsidiary Stars) calculation logic in the sanmei-core package. They provided reference tables from a book showing the correct lookup tables.

2. I launched three parallel Explore agents to understand:
   - Star calculation code (domain models, calculators, tables)
   - Star-related tests
   - Meishiki calculator and model structure

3. After analysis, I identified 4 bugs by comparing the book tables with the code:
   - Bug 1: _STAR_MAP polarity swap for KANSEI (官星) and INJYU (印綬)
   - Bug 2: _TEIOU_MAP wrong for 6 stems (all 陰干 + Earth stems)
   - Bug 3: 陰干逆行 (reverse direction for Yin stems) not implemented
   - Bug 4: Major star position mapping incorrect

4. I read all relevant source files and test files to understand the full scope of changes needed.

5. I created a PDCA state file and began the Do phase:
   - Fixed _STAR_MAP in standard.py
   - Fixed _TEIOU_MAP in standard.py
   - Added _is_yin_stem() and reverse direction logic in subsidiary_star.py
   - Fixed position mapping in major_star.py
   - Rewrote all three test files with comprehensive book table verification
   - Fixed a transcription error in the KINOTO column of the major star test

6. First `just check` failed due to ruff format issues in two test files. Fixed with `ruff format`.

7. Second `just check` passed: all 1184 tests across 3 packages, lint clean, mypy clean.

8. Updated domain docs (Chapter 4 and Chapter 5) to fix the errors that caused the bugs.

9. Updated PDCA state file with Do Log, Check Log, and Retrospective.

10. Was about to commit when the summary was requested.

Key files modified:
- packages/sanmei-core/src/sanmei_core/schools/standard.py
- packages/sanmei-core/src/sanmei_core/calculators/subsidiary_star.py
- packages/sanmei-core/src/sanmei_core/calculators/major_star.py
- packages/sanmei-core/tests/unit/schools/test_standard.py
- packages/sanmei-core/tests/unit/calculators/test_major_star.py
- packages/sanmei-core/tests/unit/calculators/test_subsidiary_star.py
- docs/domain/04_Chapter4_Ten_Major_Stars.md
- docs/domain/05_Chapter5_Twelve_Subsidiary_Stars.md
- .claude/pdca/cycle-20260302-star-fix.md

The user's only message was the initial `/task-pdca` invocation with the book reference tables. No additional user feedback was received during implementation.

Pending tasks from the PDCA skill:
- Commit the implementation code
- Commit the PDCA state file
- Update memory/learnings
- Final report

Summary:
1. Primary Request and Intent:
   The user invoked `/task-pdca` requesting a review and fix of the 十大主星 (Ten Major Stars) and 十二大従星 (Twelve Subsidiary Stars) calculation logic in the sanmei-core package. They stated these calculations were likely not following proper 算命学 (Sanmei-gaku) logic or contained bugs. They provided detailed reference tables from a book (pp.52-53) including:
   - The 十大主星表 (10×10 lookup table: 他干 × 日干 → MajorStar)
   - The 十二大従星表 (12×10 lookup table: 支 × 日干 → SubsidiaryStar)
   - The star placement diagram (3×3 grid showing positions ①-⑤ for major stars, A-C for subsidiary stars)
   - Rules for calculating positions: ①=日干×年干, ②=日干×月干, ③=日干×年支蔵干, ④=日干×月支蔵干, ⑤=日干×日支蔵干, A=日干×年支, B=日干×月支, C=日干×日支

2. Key Technical Concepts:
   - 算命学 (Sanmei-gaku / Japanese astrology) star calculation system
   - 十大主星 (Ten Major Stars) - personality stars derived from 五行関係 (five element relations) + 陰陽 (yin-yang polarity)
   - 十二大従星 (Twelve Subsidiary Stars) - fortune stars derived from 十二運 (twelve luck cycle)
   - 五行関係: 比劫(HIKAKU), 食傷(SHOKUSHOU), 財星(ZAISEI), 官星(KANSEI), 印綬(INJYU)
   - 陽順陰逆: Yang stems cycle forward through branches, Yin stems cycle backward
   - 帝旺支 (teiou branch): The branch where a stem has maximum power, differs per stem
   - 人体図 position mapping: north=①年干, south=②月干, east=③年支蔵干, center=④月支蔵干, west=⑤日支蔵干
   - 土性 (Earth element) follows 火性 (Fire element) for twelve luck cycle (戊→丙, 己→丁)
   - PDCA cycle skill for structured task execution
   - uv workspace monorepo with just commands
   - Python 3.14+, ruff strict, mypy strict

3. Files and Code Sections:

   - **`packages/sanmei-core/src/sanmei_core/schools/standard.py`** (MODIFIED)
     - Core file containing _STAR_MAP and _TEIOU_MAP lookup tables
     - Fixed _STAR_MAP: Swapped KANSEI and INJYU polarity mappings
     - Fixed _TEIOU_MAP: Corrected 6 stems (乙→寅, 丁→巳, 戊→午, 己→巳, 辛→申, 癸→亥)
     - Updated docstring to reflect correct rules
     ```python
     _TEIOU_MAP: dict[TenStem, TwelveBranch] = {
         TenStem.KINOE: TwelveBranch.U,  # 甲 → 卯
         TenStem.KINOTO: TwelveBranch.TORA,  # 乙 → 寅 (陰干: 逆行)
         TenStem.HINOE: TwelveBranch.UMA,  # 丙 → 午
         TenStem.HINOTO: TwelveBranch.MI,  # 丁 → 巳 (陰干: 逆行)
         TenStem.TSUCHINOE: TwelveBranch.UMA,  # 戊 → 午 (丙に準ずる)
         TenStem.TSUCHINOTO: TwelveBranch.MI,  # 己 → 巳 (丁に準ずる)
         TenStem.KANOE: TwelveBranch.TORI,  # 庚 → 酉
         TenStem.KANOTO: TwelveBranch.SARU,  # 辛 → 申 (陰干: 逆行)
         TenStem.MIZUNOE: TwelveBranch.NE,  # 壬 → 子
         TenStem.MIZUNOTO: TwelveBranch.I,  # 癸 → 亥 (陰干: 逆行)
     }

     _STAR_MAP: dict[tuple[GoGyoRelation, bool], MajorStar] = {
         (GoGyoRelation.HIKAKU, True): MajorStar.KANSAKU,
         (GoGyoRelation.HIKAKU, False): MajorStar.SEKIMON,
         (GoGyoRelation.SHOKUSHOU, True): MajorStar.HOUKAKU,
         (GoGyoRelation.SHOKUSHOU, False): MajorStar.CHOUJYO,
         (GoGyoRelation.ZAISEI, True): MajorStar.ROKUZON,
         (GoGyoRelation.ZAISEI, False): MajorStar.SHIROKU,
         (GoGyoRelation.KANSEI, True): MajorStar.SHAKI,  # 官星・同陰陽 → 車騎星
         (GoGyoRelation.KANSEI, False): MajorStar.KENGYU,  # 官星・異陰陽 → 牽牛星
         (GoGyoRelation.INJYU, True): MajorStar.RYUKOU,  # 印綬・同陰陽 → 龍高星
         (GoGyoRelation.INJYU, False): MajorStar.GYOKUDO,  # 印綬・異陰陽 → 玉堂星
     }
     ```

   - **`packages/sanmei-core/src/sanmei_core/calculators/subsidiary_star.py`** (MODIFIED)
     - Added `_is_yin_stem()` helper function
     - Modified `calculate_subsidiary_star()` to use reverse direction for Yin stems
     ```python
     def _is_yin_stem(stem: TenStem) -> bool:
         """陰干かを判定."""
         return stem.value % 2 == 1

     def calculate_subsidiary_star(
         day_stem: TenStem,
         target_branch: TwelveBranch,
         school: SchoolProtocol,
     ) -> SubsidiaryStar:
         """日干と対象地支から十二大従星を算出."""
         teiou = school.get_teiou_branch(day_stem)
         if _is_yin_stem(day_stem):
             distance = (teiou.value - target_branch.value) % 12
         else:
             distance = (target_branch.value - teiou.value) % 12
         return JUUNIUN_ORDER[distance]
     ```

   - **`packages/sanmei-core/src/sanmei_core/calculators/major_star.py`** (MODIFIED)
     - Fixed position mapping to match book layout (p.53)
     ```python
     def calculate_major_star_chart(
         pillars: ThreePillars,
         hidden_stems: dict[str, HiddenStems],
         school: SchoolProtocol,
     ) -> MajorStarChart:
         day_stem = pillars.day.stem
         return MajorStarChart(
             north=school.determine_major_star(day_stem, pillars.year.stem),  # ① 年干
             south=school.determine_major_star(day_stem, pillars.month.stem),  # ② 月干
             east=school.determine_major_star(day_stem, hidden_stems["year"].hongen),  # ③ 年支蔵干
             center=school.determine_major_star(day_stem, hidden_stems["month"].hongen),  # ④ 月支蔵干
             west=school.determine_major_star(day_stem, hidden_stems["day"].hongen),  # ⑤ 日支蔵干
         )
     ```

   - **`packages/sanmei-core/tests/unit/calculators/test_major_star.py`** (REWRITTEN)
     - Added full 100-pattern book table verification via parametrize
     - Updated chart position test to match new mapping
     - Each column represents a 日干, each row an 他干 → expected MajorStar

   - **`packages/sanmei-core/tests/unit/calculators/test_subsidiary_star.py`** (REWRITTEN)
     - Added full 120-pattern book table verification via parametrize
     - Note: Used calculated values for 卯 row (庚辛壬癸) due to book printing error
     - Added Yin stem specific tests (test_kinoto_at_tora_is_tenshou, etc.)
     - Updated chart test with correct 戊 帝旺=午 values

   - **`packages/sanmei-core/tests/unit/schools/test_standard.py`** (REWRITTEN)
     - Fixed TestDetermineMajorStar: swapped KANSEI/INJYU expected values
     - Fixed test_hinoe_vs_mizunoe: now expects SHAKI (車騎星) not KENGYU
     - Fixed TestGetTeiouBranch: all 10 stems with correct expected branches

   - **`docs/domain/04_Chapter4_Ten_Major_Stars.md`** (MODIFIED)
     - Fixed 官星/印綬 rows in section 4.3 table: swapped 同陰陽/異陰陽 labels

   - **`docs/domain/05_Chapter5_Twelve_Subsidiary_Stars.md`** (MODIFIED)
     - Completely revised 帝旺支 table: now shows all 10 stems individually with direction (順行/逆行)
     - Updated explanation to mention 陽順陰逆 and 土性→火性

   - **`.claude/pdca/cycle-20260302-star-fix.md`** (CREATED)
     - PDCA cycle state file with Plan, Do Log, Check Log, and Retrospective

   - Files READ but not modified: `domain/meishiki.py`, `domain/kanshi.py`, `tables/gogyo.py`, `protocols/school.py`, `domain/star.py`, `tables/hidden_stems.py`, `calculators/meishiki_calculator.py`, CLI formatters/text.py, CLI tests (conftest.py, test_text_formatter.py, test_json_formatter.py), `test_school_protocol.py`, `test_meishiki_calculator.py`

4. Errors and fixes:
   - **ruff format failure**: After writing test files, `just check` failed because `test_major_star.py` and `test_subsidiary_star.py` needed formatting. Fixed by running `uv run ruff format` on both files.
   - **KINOTO column transcription error in test_major_star.py**: Initially wrote incorrect values for the 乙 (KINOTO) column in the book table test data. Had `RYUKOU, KENGYU, SHAKI, SHIROKU, ROKUZON, GYOKUDO, HOUKAKU` where it should be `HOUKAKU, SHIROKU, ROKUZON, KENGYU, SHAKI, GYOKUDO, RYUKOU` (indices 3-9). Fixed before running tests.
   - **Book printing error identified in 卯 row**: The book's 十二大従星表 has incorrect values for 庚+卯, 辛+卯, 壬+卯, 癸+卯 (they duplicate the 寅 row values). Used algorithmically calculated values instead, verified by checking all other 11 branches for each stem match correctly.

5. Problem Solving:
   - Identified that the domain documentation (04_Chapter4, 05_Chapter5) was the ROOT CAUSE of the code bugs - the code faithfully implemented the incorrect docs
   - Discovered that the book provided by the user has a printing error in the 卯 row for the last 4 stems (庚辛壬癸), where values are duplicated from the 寅 row. Resolved by using mathematically verified values instead.
   - Unified pattern confirmed: ALL five element relations follow the same polarity rule (同陰陽→陽版/偏, 異陰陽→陰版/正), which the old code/docs incorrectly reversed for 官星 and 印綬
   - Position mapping analysis: Mapped the book's 3×3 grid to compass directions by analyzing the CLI formatter's layout (西=left, 東=right) and the book's grid positions

6. All user messages:
   - `/task-pdca` invocation with detailed argument: "十大主星,十二大従星の算出についても算命学のロジックではない可能性が高いです（または算出ロジックのバグ）。見直して、修正してください。以下の参考は書籍の記述です。" followed by the full book content including:
     - Explanation of 宿命星 calculation method (p.52)
     - The 3×3 position grid layout (p.53)
     - Complete 十大主星表 (10×10 table)
     - Complete 十二大従星表 (12×10 table)

7. Pending Tasks:
   - **Commit the implementation changes**: The code changes are complete and `just check` passes, but no git commit has been created yet
   - **Commit the PDCA state file**: Per PDCA skill, `.claude/pdca/` should be committed separately with `chore: add PDCA cycle record for [description]`
   - **Feedback phase**: Save learnings to memory file (`pdca-learnings.md`) and provide final report to user
   - **Update PDCA state to completed**: Change status from `in_progress` to `completed`

8. Current Work:
   The PDCA cycle is in the Retrospective→Feedback transition. All implementation is complete:
   - All 4 bugs fixed (star map polarity, teiou map, yin stem reverse, position mapping)
   - All tests pass (570 sanmei-core, 549 dynamic-ontology, 65 sanmei-cli = 1184 total)
   - Domain docs updated
   - PDCA state file written with full retrospective
   
   I was about to proceed with the PDCA Feedback phase which includes:
   1. Saving learnings to memory
   2. Committing implementation code (`fix(sanmei-core): ...`)
   3. Committing PDCA state file (`chore: add PDCA cycle record for ...`)
   4. Delivering final report to the user

9. Optional Next Step:
   Continue the PDCA Feedback phase: commit the implementation changes and the PDCA state file, save learnings to memory, and deliver the final PDCA completion report. This is directly in line with the task-pdca skill's defined workflow, following the Check PASS → Retrospective (completed) → Feedback phase.

   The PDCA state file shows: `Status: retrospective` and all DoD items checked. The next step per the PDCA skill is:
   - "ナレッジ保存: 振り返りの「学び」と「再発防止策」をメモリファイルに保存"
   - "状態ファイルを Status: completed に更新"
   - "PDCA 状態ファイルをコミット"
   - "最終報告"

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/toguchi/.claude/projects/-Users-toguchi-PARA-Projects-Private-dynamic-sanmei/45633fd0-4198-4f94-a827-f08a28c474a4.jsonl
Please continue the conversation from where we left off without asking the user any further questions. Continue with the last task that you were asked to work on.